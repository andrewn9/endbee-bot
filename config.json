{
  "commands": [
    {
      "name": "ping",
      "description": "Ping command"
    },
    {
      "name": "start",
      "description": "Start a game with another user",
      "options": [
        {
          "name": "user",
          "description": "The user to start a game with",
          "type": 6,
          "required": true
        }
      ]
    },
    {
      "name": "search",
      "description": "Search for words beginning with the arguement",
      "options": [
        {  
          "name": "query",
          "description": "The beginning of the word",
          "type": 3,
          "required": true
        }
      ]
    }
  ]
}
